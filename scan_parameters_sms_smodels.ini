[options]
cleanOutput = True
runMadGraph = True
runConvertSLHA = True
runPythia = False
runDelphes = False
runMadSpin = False
ncpu = 10
ncore = 4
[AuxPars]
massPairs = $loop{[(5.0, 300.0), (5.0, 400.0)]}
[MadGraphPars]
#MG5 Cards:
proccard = Cards/SMS-stop/proc_card_sms.dat # Optional (only used if processFolder does not exist)
paramcard = Cards/SMS-stop/param_card.dat # Path to the default parameter_card.dat to be used by MadGraph. Note that the parameter values defined in the MadGraphSet section below will automatically overwrite the values in paramcard. If not defined, the default file for the model will be used.
runcard = Cards/SMS-stop/run_card_sms.dat # Path to the default run_card.dat to be used by MadGraph. Note that the parameter values defined in the MadGraphSet section below will automatically overwrite the values in runcard. If not defined, the default file for the model will be used.
delphescard = Cards/delphes_card_trimmed.dat # If not defined, will not run Delphes
pythia8card = Cards/pythia8_card.dat # If not defined it will not run Pythia and Delphes
processFolder = processFolders/pp2STST_smodels # Path to the folder to store the process generation output. If it already exists, the process generation will be #List of model parameters. Only values in this section are allowed as lists (for loops)
[MadGraphSet]
nevents = 2000
mST = ${AuxPars:massPairs}[1]
mChi = ${AuxPars:massPairs}[0]
mt = 172.5
ydm = 1.0
wST = 'auto'
run_tag = "sms_%1.0f_%1.0f" %(${mST},${mChi})
