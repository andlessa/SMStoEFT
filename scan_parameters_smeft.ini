[options]
cleanOutput = True
runMadGraph = True
runConvertSLHA = False
runPythia = True
runDelphes = True
ncpu = 3
ncore = 15
[AuxPars]
massPairs = $loop{[(700.0, 600.0), (800.0, 700.0), (1000.0, 900.0), (1200.0, 1100.0), (1500.0, 1400.0), (2000.0, 1900.0), (2500.0, 2400.0), (3000.0, 2900.0)]}
[MadGraphPars]
#MG5 Cards:
proccard = Cards/proc_card_smeft_full.dat # Optional (only used if processFolder does not exist)
paramcard = Cards/param_card.dat # Path to the default parameter_card.dat to be used by MadGraph. Note that the parameter values defined in the MadGraphSet section below will automatically overwrite the values in paramcard. If not defined, the default file for the model will be used.
runcard = Cards/run_card_smeft.dat # Path to the default run_card.dat to be used by MadGraph. Note that the parameter values defined in the MadGraphSet section below will automatically overwrite the values in runcard. If not defined, the default file for the model will be used.
extraCard = Cards/FKS_params.dat
delphescard = Cards/delphes_card_trimmed.dat # If not defined, will not run Delphes
pythia8card = Cards/pythia8_card.dat # If not defined it will not run Pythia and Delphes
processFolder = processFolders/pp2TT_bsm # Path to the folder to store the process generation output. If it already exists, the process generation will be #List of model parameters. Only values in this section are allowed as lists (for loops)
[MadGraphSet]
nevents = 50000
mST = ${AuxPars:massPairs}[0]
mChi = ${AuxPars:massPairs}[1]
ydm = 100.0
wST = 'auto'
run_tag = "smeft_bsm_%1.0f_%1.0f" %(${mST},${mChi})
